# Установить основной образ
FROM node:16-alpine

# Установить рабочую директорию для приложения
WORKDIR /app

# Копировать package.json и package-lock.json
COPY package*.json ./

# Установить зависимости приложения
RUN npm ci

# Копировать содержимое приложения в рабочую директорию
COPY . .

# Открыть порт для входящих подключений
EXPOSE 3000

# Запустить приложение
CMD [ "npm", "start" ]  